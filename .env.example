APP_NAME=Construction-Tool
APP_ENV=local
APP_KEY=base64:YLakyPouqr/fp9is1QERx1JYiw/s4D6cNGatRNU5hJU=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=construction_tool
DB_USERNAME=sail
DB_PASSWORD=password

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration for Testing
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@macocoding.com
MAIL_FROM_NAME="${APP_NAME} Team"

VITE_APP_NAME="${APP_NAME}"

# --- Project identity (prevents network/volume/name collisions)
COMPOSE_PROJECT_NAME=construction_tool_filament


# --- App & Vite
APP_PORT=8085
VITE_PORT=5185

# --- Datastores & utilities
FORWARD_DB_PORT=33075 # MySQL (container 3306)
FORWARD_REDIS_PORT=6385 # Redis (container 6379)
FORWARD_MEILISEARCH_PORT=7705

# --- Mailpit
FORWARD_MAILPIT_PORT=1125 # SMTP (container 1025)
FORWARD_MAILPIT_DASHBOARD_PORT=8125 # Web UI (container 8025)

# --- MinIO
FORWARD_MINIO_PORT=9005 # S3 API (container 9000)
FORWARD_MINIO_CONSOLE_PORT=8905 # Console (container 8900)
